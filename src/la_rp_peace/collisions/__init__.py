"""Stage 4.1: function collisions inside one document (methodology/04_1_function_collisions.md).

``sides`` builds the compared assignments (function/duty records and consolidated joint views),
``pairs`` finds the pairs allowed by the two search paths and keeps those strictly above the
similarity threshold, ``prompt`` renders the verification questions, ``checks`` validates the
model's answers, ``store`` replaces the stage's rows and ``pipeline`` runs it all as a stage.
"""
