# Спецификация продукта

| Файл | Что это | Для чего |
|---|---|---|
| [`tz_site.md`](tz_site.md) | ТЗ для разработки | **Что** делать и **в каком порядке**: инварианты, пайплайн, API, модель данных, страницы, уровни приоритета P0–P3 |
| [`visual_compare.html`](visual_compare.html) | Интерактивный макет экрана «Изменения → Визуал» | **Как** это выглядит и ведёт себя; демо-данные для фикстуры `fixtures/result.json` |

Исходное задание организаторов — отдельный документ; `tz_site.md` его детализирует и
расставляет приоритеты.

## Как работать

1. **Перед задачей** прочитайте в `tz_site.md` разделы §0 (порядок работы), §2 (инварианты
   I1–I7) и §8 (уровни приоритета).
2. **Берите задачу по приоритету.** Сначала P0, потом P1, P2, P3. Следующий уровень
   начинается, только когда выполнены критерии приёмки текущего. Критерий из §8 — это и есть
   «готово».
3. **Инварианты не нарушаются ни на одном уровне.** Особенно I1 (нет вывода без источника) и
   I2 (цитата дословная, проверяется кодом).
4. **Интерфейс сверяйте с макетом:** `xdg-open docs/spec/visual_compare.html` (или откройте
   файл в браузере). Макет самодостаточный, сеть не нужна. Наведите курсор на карточку,
   кликните по ней, кликните по цитате в панели.
5. **Код макета не копируйте.** Это ванильный JS с `innerHTML`, написанный для демонстрации.
   Интерфейс собирается на стеке из [`.agents/frontend.md`](../../.agents/frontend.md) §1.

## Что показывает макет

Это эталон для P0.8 (ТЗ §6.3):

- сводка: счётчики по типам и «Показать полную аналитику»;
- по каждому блоку три колонки: «до» | карточки изменений | «после»;
- стрелки «до» → карточка → «после», цвет по типу; `also` рисуется пунктиром, потеря —
  «∅ не закреплено»;
- наведение на карточку или пункт подсвечивает всю связку и приглушает остальное;
- клик по карточке открывает панель доказательной базы: вывод → как получен → цепочка
  доказательств (шаги-факты с цитатами, шаги-рассуждения с пометкой «рассуждение агента») →
  что проверено (кандидаты со сходством и порогом) → нормативная база → уверенность →
  «Подтвердить / Отклонить»;
- клик по цитате закрывает панель, прокручивает к пункту и подсвечивает его;
- цвета типов — переменные в `:root` (`--kept`, `--transformed`, `--created`, `--moved`,
  `--loss`, `--duplication`, `--conflict`, `--overlap`).

В макете **нет**: режима Word (там заглушка), страниц «Загрузка», «Статус», «Анализ»,
«Чат». Их делаем по тексту ТЗ §6. Демо-данные вымышленные (ДИБиР, ЦЦТ…), они не из
документов в `test_data/`.

## Демо-данные → `fixtures/result.json`

Массив `DATA` в `<script>` макета — исходник фикстуры из ТЗ §5.3. Поля макета называются
короче, чем в ТЗ §5; при переносе:

| В макете | В `result.json` (ТЗ §5) |
|---|---|
| блок `{title, before, after, changes: [объекты]}` | `blocks[]`: `{title, before, after, changes: ["C-001", …]}`; сами изменения — в `findings` по id |
| пункт `{id, label, st}` | `{id, label, status, clause_id}` |
| изменение `ev` | `evidence` |
| шаг `{t, src, inf}` | `{kind: "inference"` если `inf`, иначе `"fact", text, sources}` |
| источник `s("До"/"После", block, clause, quote, item)` | `{doc: "before"/"after", block, clause_id, clause, quote, item}` |
| `checked.cands: [[label, clause, score, item]]` | `checked.candidates: [{label, clause, score, item}]` |
| `conf`, `confNote` | `confidence`, `confidence_note` |
| глобальный `review[id]` | поле `review` у находки |
| `compact: true` | не переносится: это вид карточки для `type: "kept"` |

В макете нет `clause_id` — для фикстуры их нужно назначить (формат `after:dibr:2.8`, ТЗ
§5.1). Цитаты в макете обрамлены «…»: в фикстуре многоточия убираются, цитата должна
дословно входить в текст пункта (I2).

## Расхождения с другими документами репо

Пока не решены командой; до решения ни одна сторона не считается правильной.

| Тема | `tz_site.md` | Сейчас в репо |
|---|---|---|
| API | `POST /api/jobs` с обоими файлами, `/status`, `/result`, `/review`, `/chat` (§4) | бэкенд реализовал `/api/documents` (по файлу), `/api/analyses` в планах — [`backend.md`](../../.agents/backend.md), [`frontend.md`](../../.agents/frontend.md) §4 |
| Работа фронта до готовности бэкенда | по фикстуре `fixtures/result.json` (§0.3) | [`AGENTS.md`](../../AGENTS.md) и `frontend.md` §4 запрещают писать замену чужому коду и мок-бэкенд |
| Страницы | «Изменения (Визуал / Word) · Анализ · Чат», выгрузки в файл нет (§6) | `frontend.md` §2: вкладки «Подразделения / Функции / Потери / …», экспорт DOCX/PDF, история |
| Загрузка | два поля, «До» и «После» (§6.1); нормативка и операторы — P3 | экран `/` в `frontend/` принимает по нескольку файлов в 4 зоны |
| Визуальный стиль | тёмная тема макета | в `frontend/` светлая тема |
